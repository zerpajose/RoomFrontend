const styles = {
    logo: {
        fontWeight: '700x',
        fontSize: '2rem',
    }
}

const Logo = () => {
  return(
      <div style={styles.logo}>
          Shop
      </div>
  )
}

export default Logo;